// This needs the JQuery Library to be loaded.

// jquery ready start
$(document).ready(function () {
    // jQuery code

    // add padding top to show content behind navbar
    $('body').css('padding-top', $('.navbar').outerHeight() + 'px');

}); // jquery end